"use strict";
//program 1 defining function using arrow function
let multiple = (x, y) => {
    return x + y;
};
console.log(multiple(12, 5));
// program 2
let addtionH = (x, y) => { return x + y; };
// program 3
class Details {
    constructor() {
        this.value = 2;
        this.display = () => {
            return "welcome";
        };
    }
}
// program 4
// function declaration example
function additionL(x, y) {
    return x + y;
}
// function expression
let additionX = function (x, y) {
    return x + y;
};
// arrow function
let additionH = (x, y) => {
    return x + y;
};
// Or 
let addNumb = (x, y) => x + y;
let Kajal = {
    firstName: 'Kajal',
    lastName: 'Dule',
    rollNo: 23
};
let SapnaA = {
    subject: 'FD',
    attendance: 'Full',
    remarks: 'Good performance'
};
let SurajA = {
    subject: 'History',
    attendance: 'fifty Percent'
};
let pointA = {
    x: 12,
    y: 13
};
//pointA.x = 133
